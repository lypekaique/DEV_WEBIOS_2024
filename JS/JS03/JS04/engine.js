let navegador = 'Brave'
let novaString = navegador.substring(2) // começa do 2 até o final
let novaSubString = navegador.substring(1, 3) // corta até onde eu quero
let cutl = navegador.charAt(3) // corta um caractere
//split "frase.split()" separa os espaços como se fosse array
